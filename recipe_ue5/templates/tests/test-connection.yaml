apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "recipe_ue5.fullname" . }}-test-connection"
  labels:
    {{- include "recipe_ue5.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "recipe_ue5.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
